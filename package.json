{
  "name": "private-messenger",
  "private": true,
  "version": "2.0.0",
  "description": "End-to-end encrypted messenger on the Signal protocol, running on Cloudflare.",
  "scripts": {
    "install:all": "cd client && npm install && cd ../worker && npm install",
    "test": "cd worker && npm test && cd ../client && npm test",
    "build": "echo \"Wrong directory: this repository root builds neither half. Set the Cloudflare project Root directory to worker (the server) or client (the app).\" && exit 1",
    "dev:worker": "cd worker && npm run dev",
    "dev:client": "cd client && npm run dev",
    "e2e": "node e2e/run.mjs",
    "build:client": "cd client && npm install && npm run build"
  }
}
